# Reverse a string using functions
def my_function(x): #def is the keyword used to define a function. 
  return x[::-1]

mytxt = my_function("I wonder how this text looks like backwards")

print(mytxt)
